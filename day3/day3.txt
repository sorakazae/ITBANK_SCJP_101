break나 continue문 뒤에 라벨을 붙여 for문 앞에 붙은 라벨과 같은 것을 break나 continue 할 수 있다.


boolean형의 기본값은 false

배열이름.length 는 배열의 크기를 알려준다.
2차원 배열은 행의 배열을 참조한다.

행마다 열의 개수가 다른것이 가변배열 (c의 포인터 배열과 비슷하다) 

아규먼트 값 주는 법
Run Configurations...  ->  Arguments

반복되는 문장은 메서드로 빼는 것이 좋다.